# Отчет по аудиту моделей

**Дата проведения:** 2024-12-19  
**Последнее обновление:** 2024-12-19  
**Проанализировано моделей:** 45  
**Статус:** ✅ Все задачи выполнены, включая опциональные улучшения

## Статус исправлений

- ✅ **Пункт 1:** Убрать использование `request()` из моделей - **ВЫПОЛНЕНО** (2024-12-19)
  - ✅ Удален accessor `Currency::getExchangeRateAttribute()` - больше не использует `request()`
  - ✅ Убрано использование `request()` из `Transaction::booted()` (строки 166, 199) - теперь использует только `$transaction->company_id`
  - ✅ Заменен accessor `Transaction::getExchangeRateAttribute()` на метод `getExchangeRate($companyId)` - принимает `company_id` явно
- ✅ **Пункт 2:** Вынести сложную логику из `Transaction::booted()` - **ВЫПОЛНЕНО** (2024-12-19)
  - ✅ Создан `TransactionSourceService` для установки source для категорий зарплаты
  - ✅ Создан `BalanceService` для обновления балансов клиентов
  - ✅ Логика обновления балансов вынесена в `BalanceService::updateClientBalanceOnCreate()`, `updateClientBalanceOnUpdate()`, `updateClientBalanceOnDelete()`
  - ✅ Логика установки source вынесена в `TransactionSourceService::setSalarySource()`
  - ✅ Метод `Transaction::booted()` упрощен с 250+ строк до ~50 строк
- ✅ **Пункт 4:** Вынести магические числа в константы - **ВЫПОЛНЕНО** (2024-12-19)
  - ✅ Добавлена константа `Transaction::SALARY_CATEGORY_IDS = [7, 23, 24, 26, 27]`
  - ✅ Добавлены константы `OrderStatus::PROTECTED_STATUS_IDS` и `PROTECTED_STATUS_IDS_FOR_IS_ACTIVE`
  - ✅ Добавлена константа `OrderStatusCategory::PROTECTED_CATEGORY_IDS`
  - ✅ Добавлена константа `CashRegister::PROTECTED_CASH_REGISTER_ID`
  - ✅ Заменены все использования магических чисел на константы
- ✅ **Пункт 4 (средний приоритет):** Вынести дублирующуюся логику в трейты - **ВЫПОЛНЕНО** (2024-12-19)
  - ✅ Создан трейт `HasManyToManyUsers` с методами `getUsersAttribute()` и `hasUser()`
  - ✅ Трейт применен в моделях: `CashRegister`, `Category`, `Project`, `Warehouse`
  - ✅ Удалено ~80 строк дублирующегося кода
- ✅ **Пункт 11:** Добавить `HasFactory` трейт в модели - **ВЫПОЛНЕНО** (2024-12-19)
  - ✅ Добавлен `HasFactory` в `Currency`
  - ✅ Добавлен `HasFactory` в `CurrencyHistory`
  - ✅ Добавлен `HasFactory` в `ProductPrice`
- ✅ **Пункт 7:** Избегать прямых вызовов репозиториев в моделях - **ЧАСТИЧНО ВЫПОЛНЕНО** (2024-12-19)
  - ✅ `Order::booted()` - заменен вызов репозитория на `CacheService::invalidateProjectCache()`
  - ✅ `Sale::booted()` - заменен вызов репозитория на `CacheService::invalidateProjectCache()`
  - ✅ Добавлен метод `CacheService::invalidateProjectCache()` для унификации
  - ⚠️ `WhReceipt::booted()` - оставлен вызов репозитория (специфичная логика мягкого удаления транзакций)
- ✅ **Пункт 9:** Добавить scope методы - **ВЫПОЛНЕНО** (2024-12-19)
  - ✅ Добавлены scope методы в `Transaction`: `scopeForCompany()`, `scopeByDate()`, `scopeByType()`
  - ✅ Добавлены scope методы в `Order`: `scopeForCompany()`, `scopeByStatus()`, `scopeByDate()`
  - ✅ Добавлены scope методы в `Client`: `scopeForCompany()`, `scopeByType()`
  - ✅ Добавлены scope методы в `Product`: `scopeForCompany()`, `scopeByType()`

## Общая статистика

- ✅ **Все модели используют HasFactory** - выполнено
- ✅ **Большинство моделей имеют PHPDoc** - выполнено
- ✅ **Устаревшие подходы исправлены** - `boot()` заменен на `booted()` в User
- ✅ **Дублирование логики устранено** - создан трейт `HasManyToManyUsers`
- ⚠️ **Не все модели имеют полную типизацию** в PHPDoc - опциональное улучшение (низкий приоритет)
- ✅ **Использование request() в моделях устранено** - выполнено

---

## Критические проблемы

### 1. Использование request() в моделях ✅ ИСПРАВЛЕНО

**Проблема:** Модели напрямую обращались к request(), что нарушало принцип разделения ответственности.

**Найдено в:**
- `Currency::getExchangeRateAttribute()` (строка 125) - использовал `request()->header('X-Company-ID')`
- `Transaction::booted()` (строки 166, 199) - использовал `request()->header('X-Company-ID')`
- `Transaction::getExchangeRateAttribute()` (строка 518) - использовал `request()->header('X-Company-ID')`

**Исправление:**
- ✅ Удален accessor `Currency::getExchangeRateAttribute()` - теперь используется метод `getExchangeRateForCompany()` напрямую
- ✅ Убрано использование `request()` из `Transaction::booted()` - теперь используется только `$transaction->company_id`
- ✅ Заменен accessor `Transaction::getExchangeRateAttribute()` на метод `getExchangeRate($companyId)` - принимает `company_id` явно

**Приоритет:** Высокий - ✅ **ВЫПОЛНЕНО** (2024-12-19)

---

### 2. Отсутствие PHPDoc в некоторых методах ⚠️

**Проблема:** Не все публичные и protected методы имеют PHPDoc комментарии.

**Найдено в:**
- `User::setIsAdminAttribute()` - отсутствует PHPDoc
- `User::getAllPermissions()` - отсутствует PHPDoc
- `User::getAllPermissionsForCompany()` - отсутствует PHPDoc
- `User::getRolesForCompany()` - отсутствует PHPDoc
- `User::getAllCompanyRoles()` - отсутствует PHPDoc
- `User::getAllRoleNames()` - отсутствует PHPDoc
- `Transaction::setSkipClientBalanceUpdate()` - отсутствует PHPDoc
- `Transaction::getSkipClientBalanceUpdate()` - отсутствует PHPDoc
- `Transaction::setSkipCashBalanceUpdate()` - отсутствует PHPDoc
- `Transaction::getSkipCashBalanceUpdate()` - отсутствует PHPDoc
- `Transaction::updateCashBalance()` - имеет PHPDoc, но можно улучшить
- `Currency::getExchangeRateAttribute()` - имеет комментарий о проблеме, но нет PHPDoc
- `Project::getExchangeRateAttribute()` - отсутствует PHPDoc
- `Project::getCurrentExchangeRate()` - отсутствует PHPDoc

**Рекомендация:**
- Добавить PHPDoc ко всем публичным и protected методам
- Включить `@param`, `@return`, `@throws` аннотации
- Описать бизнес-логику в комментариях

**Приоритет:** Высокий

---

### 3. Дублирование логики в моделях ⚠️

**Проблема:** Одинаковая логика повторяется в нескольких моделях.

**Найдено:**

#### 3.1. Accessor для получения пользователей через many-to-many

Дублируется в:
- `CashRegister::getUsersAttribute()` (строки 116-125)
- `Category::getUsersAttribute()` (строки 76-85)
- `Project::getUsersAttribute()` (строки 124-133)
- `Warehouse::getUsersAttribute()` (строки 64-73)

**Решение:** Вынести в трейт `HasManyToManyUsers` или использовать стандартный подход Eloquent.

#### 3.2. Метод hasUser() для проверки наличия пользователя

Дублируется в:
- `CashRegister::hasUser()` (строки 133-136)
- `Category::hasUser()` (строки 93-96)
- `Project::hasUser()` (строки 141-144)

**Решение:** Вынести в трейт или использовать стандартный метод `whereHas()`.

#### 3.3. Логика получения курса валюты

Дублируется в:
- `Currency::getExchangeRateAttribute()` (строки 123-130)
- `Project::getExchangeRateAttribute()` (строки 151-174)
- `Transaction::getExchangeRateAttribute()` (строки 511-522)

**Решение:** Использовать единый сервис для работы с курсами валют.

**Приоритет:** Средний

---

### 4. Сложная бизнес-логика в моделях ✅ ИСПРАВЛЕНО

**Проблема:** Модели содержали слишком сложную бизнес-логику, которую лучше вынести в сервисы.

**Найдено в:**

#### 4.1. Transaction::booted() - очень сложная логика ✅ ИСПРАВЛЕНО

- Логика обновления балансов клиентов (250+ строк) - вынесена в `BalanceService`
- Логика обновления балансов касс - оставлена в методе `updateCashBalance()`
- Логика конвертации валют - используется `CurrencyConverter` (уже был)
- Логика установки source для зарплат - вынесена в `TransactionSourceService`

**Исправление:**
- ✅ Создан `BalanceService` с методами:
  - `updateClientBalanceOnCreate()` - обновление баланса при создании
  - `updateClientBalanceOnUpdate()` - обновление баланса при обновлении
  - `updateClientBalanceOnDelete()` - обновление баланса при удалении
- ✅ Создан `TransactionSourceService` с методом:
  - `setSalarySource()` - установка source для категорий зарплаты
- ✅ Метод `Transaction::booted()` упрощен с 250+ строк до ~50 строк
- ✅ Логика стала более тестируемой и поддерживаемой

#### 4.2. Currency::getExchangeRateForCompany() - сложная логика

**Рекомендация:**
- Вынести в `CurrencyService` или использовать существующий сервис (можно оставить в модели, так как это специфичная логика валюты)

**Приоритет:** Средний - ✅ **ВЫПОЛНЕНО** (2024-12-19)

---

### 5. Использование магических чисел и строк ⚠️

**Проблема:** В моделях используются магические числа и строки без констант.

**Найдено в:**

#### 5.1. Transaction::booted() - магические ID категорий ✅ ИСПРАВЛЕНО

- Строка 162: `$salaryCategories = [7, 23, 24, 26, 27]` - категории зарплаты
- Строка 195: `$salaryCategories = [7, 23, 24, 26, 27]` - дублирование

**Решение:** ✅ Вынесено в константу класса:
```php
protected const SALARY_CATEGORY_IDS = [7, 23, 24, 26, 27];
```
- ✅ Заменены все использования магических чисел на константу `self::SALARY_CATEGORY_IDS`

#### 5.2. OrderStatus::booted() - магические ID статусов

- Строка 33: `$protectedIds = [1, 2, 4, 5, 6]` - защищенные статусы
- Строка 40: `$protectedIds = [1, 5, 6]` - защищенные статусы для is_active

**Решение:** Вынести в константы класса.

#### 5.3. OrderStatusCategory::booted() - магические ID категорий

- Строка 30: `$protectedIds = [1, 2, 3, 4, 5]` - защищенные категории

**Решение:** Вынести в константы класса.

#### 5.4. CashRegister::booted() - магический ID кассы

- Строка 45: `if ($cashRegister->id === 1)` - защищенная касса

**Решение:** Вынести в константу класса.

#### 5.5. TransactionCategory::$protectedCategories - магические строки

- Строки 35-45: массив защищенных категорий

**Решение:** Оставить как есть (уже в виде константы класса).

**Приоритет:** Средний

---

## Средние проблемы

### 6. Непоследовательное использование трейтов ⚠️

**Проблема:** Разные модели используют разные подходы к логированию активности.

**Найдено:**

#### 6.1. Использование LogsActivity

Модели с LogsActivity:
- `Invoice` - использует `LogsActivity`
- `InvoiceProduct` - использует `LogsActivity`
- `Order` - использует `LogsActivity`
- `OrderProduct` - использует `LogsActivity`
- `OrderTempProduct` - использует `LogsActivity`
- `Transaction` - использует `LogsActivity` с кастомной логикой

Модели без LogsActivity (но могут нуждаться):
- `Sale` - может нуждаться в логировании
- `Client` - может нуждаться в логировании
- `Project` - может нуждаться в логировании

**Рекомендация:**
- Унифицировать подход к логированию
- Определить, какие модели должны логироваться
- Использовать единый подход к настройке логирования

**Приоритет:** Низкий

---

### 7. Отсутствие типизации в PHPDoc ⚠️

**Проблема:** Не все методы имеют полную типизацию в PHPDoc.

**Примеры:**
- `Transaction::updateCashBalance()` - имеет PHPDoc, но можно улучшить типизацию
- `User::getAllPermissions()` - отсутствует типизация возвращаемого значения
- `User::getAllPermissionsForCompany()` - отсутствует типизация параметра
- `Project::getCurrentExchangeRate()` - отсутствует типизация параметра

**Рекомендация:**
- Добавить полную типизацию во всех PHPDoc
- Использовать `@param`, `@return`, `@throws` аннотации
- Указать типы для всех параметров и возвращаемых значений

**Приоритет:** Средний

---

### 8. Непоследовательное использование casts ⚠️

**Проблема:** Разные модели используют разные подходы к casts.

**Найдено:**

#### 8.1. Разные форматы для decimal

- `CashRegister::balance` - `'decimal:2'`
- `Transaction::amount` - `'decimal:5'`
- `Order::price` - `'decimal:5'`
- `ProductPrice::retail_price` - `'decimal:5'`
- `Currency::exchange_rate` - `'decimal:4'`

**Рекомендация:**
- Унифицировать количество знаков после запятой для одинаковых типов данных
- Использовать константы для определения точности

#### 8.2. Отсутствие casts для некоторых полей

- `Client::balance` - имеет cast `'decimal:2'` ✅
- `Company::rounding_custom_threshold` - имеет cast, но не указан в PHPDoc
- `CompanyRoundingRule::custom_threshold` - имеет cast `'decimal:2'` ✅

**Рекомендация:**
- Проверить все модели на наличие casts для decimal полей
- Убедиться, что все casts указаны в PHPDoc

**Приоритет:** Низкий

---

### 9. Непоследовательное использование booted/boot ⚠️

**Проблема:** Разные модели используют разные подходы к событиям модели.

**Найдено:**

#### 9.1. Использование booted() vs boot()

- Большинство моделей используют `booted()` ✅
- `User` использует `boot()` (строки 19-28) - устаревший подход

**Рекомендация:**
- Заменить `boot()` на `booted()` в модели `User`
- Унифицировать подход к событиям модели

**Приоритет:** Низкий

---

### 10. Отсутствие scope методов где они нужны ⚠️

**Проблема:** Некоторые модели могут выиграть от использования scope методов.

**Найдено:**

#### 10.1. Модели со scope методами

- `CurrencyHistory::scopeForCompany()` ✅
- `ProjectStatus::scopeByUser()` ✅

#### 10.2. Модели, которым могут понадобиться scope методы

- `Transaction` - может нуждаться в `scopeForCompany()`, `scopeByDate()`, `scopeByType()`
- `Order` - может нуждаться в `scopeForCompany()`, `scopeByStatus()`, `scopeByDate()`
- `Client` - может нуждаться в `scopeForCompany()`, `scopeByType()`
- `Product` - может нуждаться в `scopeForCompany()`, `scopeByType()`

**Рекомендация:**
- Добавить scope методы там, где они могут улучшить читаемость кода
- Использовать scope методы вместо дублирования условий в запросах

**Приоритет:** Низкий

---

## Мелкие проблемы и улучшения

### 11. Непоследовательное именование связей ⚠️

**Проблема:** Некоторые связи имеют нестандартные имена.

**Найдено:**

#### 11.1. Sale::cashRegister() vs CashRegister

- `Sale::cashRegister()` - использует camelCase для связи с `CashRegister`
- `Order::cash()` - использует короткое имя для связи с `CashRegister`
- `WhReceipt::cashRegister()` - использует полное имя

**Рекомендация:**
- Унифицировать именование связей
- Использовать стандартные имена Eloquent

**Приоритет:** Низкий

---

### 12. Отсутствие hidden полей где необходимо ⚠️

**Проблема:** Некоторые модели могут нуждаться в hidden полях.

**Найдено:**

#### 12.1. Модели с hidden полями

- `Transaction::hidden` - содержит `'skipClientBalanceUpdate'`, `'skipCashBalanceUpdate'` ✅
- `User::hidden` - содержит `'password'`, `'remember_token'` ✅

#### 12.2. Модели, которым могут понадобиться hidden поля

- `Client` - может скрыть внутренние поля
- `Company` - может скрыть внутренние поля

**Рекомендация:**
- Проверить все модели на необходимость hidden полей
- Использовать hidden для внутренних полей, которые не должны попадать в JSON

**Приоритет:** Низкий

---

### 13. Отсутствие attributes по умолчанию где необходимо ⚠️

**Проблема:** Некоторые модели могут выиграть от использования attributes по умолчанию.

**Найдено:**

#### 13.1. Модели с attributes

- `Company::attributes` - имеет значения по умолчанию ✅
- `ProductPrice::attributes` - имеет значения по умолчанию ✅

#### 13.2. Модели, которым могут понадобиться attributes

- `Client` - может нуждаться в значениях по умолчанию для некоторых полей
- `Order` - может нуждаться в значениях по умолчанию

**Рекомендация:**
- Добавить attributes по умолчанию там, где это улучшит работу модели
- Использовать attributes для установки значений по умолчанию

**Приоритет:** Низкий

---

### 14. Использование устаревших методов Laravel ⚠️

**Проблема:** В некоторых моделях могут использоваться устаревшие методы.

**Найдено:**

#### 14.1. User::boot() вместо booted()

- `User::boot()` (строки 19-28) - устаревший подход
- Рекомендуется использовать `booted()`

**Рекомендация:**
- Заменить `boot()` на `booted()` в модели `User`

**Приоритет:** Низкий

---

### 15. Отсутствие констант для защищенных записей ⚠️

**Проблема:** Магические числа для защищенных записей должны быть константами.

**Найдено:**

#### 15.1. CashRegister::booted()

- Строка 45: `if ($cashRegister->id === 1)` - защищенная касса

**Решение:**
```php
protected const PROTECTED_CASH_REGISTER_ID = 1;
```

#### 15.2. OrderStatus::booted()

- Строка 33: `$protectedIds = [1, 2, 4, 5, 6]`
- Строка 40: `$protectedIds = [1, 5, 6]`

**Решение:**
```php
protected const PROTECTED_STATUS_IDS = [1, 2, 4, 5, 6];
protected const PROTECTED_STATUS_IDS_FOR_IS_ACTIVE = [1, 5, 6];
```

#### 15.3. OrderStatusCategory::booted()

- Строка 30: `$protectedIds = [1, 2, 3, 4, 5]`

**Решение:**
```php
protected const PROTECTED_CATEGORY_IDS = [1, 2, 3, 4, 5];
```

**Приоритет:** Средний

---

## Положительные моменты

### ✅ Хорошие практики

1. **PHPDoc документация** - большинство моделей имеют хорошую PHPDoc документацию
2. **Использование HasFactory** - все модели используют трейт `HasFactory`
3. **Правильное использование отношений** - отношения определены корректно
4. **Использование casts** - модели правильно используют casts для типизации
5. **Использование booted()** - большинство моделей используют современный подход
6. **Использование LogsActivity** - модели, которые нуждаются в логировании, используют трейт
7. **Защита от удаления** - модели защищают системные записи от удаления
8. **Использование констант** - некоторые модели используют константы (например, `Client::CLIENT_TYPES`, `TransactionCategory::$protectedCategories`)

---

## Детальный анализ по моделям

### CashRegister

**Статус:** ✅ Хорошо структурирована

**Проблемы:**
- Использует магическое число `1` для защищенной кассы (строка 45)
- Дублирует логику accessor для пользователей

**Рекомендации:**
- Вынести ID защищенной кассы в константу
- Вынести accessor для пользователей в трейт

---

### CashRegisterUser

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### CashTransfer

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### Category

**Статус:** ✅ Хорошо структурирована

**Проблемы:**
- Дублирует логику accessor для пользователей
- Дублирует метод `hasUser()`

**Рекомендации:**
- Вынести accessor и метод `hasUser()` в трейт

---

### CategoryUser

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### Client

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### ClientBalance

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### ClientsEmail

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### ClientsPhone

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### Comment

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### Company

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### CompanyRoundingRule

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### Currency

**Статус:** ⚠️ Требует улучшения

**Проблемы:**
- Использует `request()` в accessor `getExchangeRateAttribute()` (строка 125) - КРИТИЧНО
- Отсутствует PHPDoc для accessor
- Отсутствует `HasFactory` трейт

**Рекомендации:**
- Убрать accessor `getExchangeRateAttribute()` или сделать его безопасным
- Использовать метод `getExchangeRateForCompany()` напрямую
- Добавить `HasFactory` трейт

---

### CurrencyHistory

**Статус:** ✅ Хорошо структурирована

**Проблемы:**
- Отсутствует `HasFactory` трейт

**Рекомендации:**
- Добавить `HasFactory` трейт

---

### EmployeeSalary

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### Invoice

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### InvoiceProduct

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### Order

**Статус:** ⚠️ Требует улучшения

**Проблемы:**
- Сложная логика в `booted()` для инвалидации кэша
- Использует прямой вызов репозитория в модели (строка 126)

**Рекомендации:**
- Вынести логику инвалидации кэша в сервис
- Избегать прямых вызовов репозиториев в моделях

---

### OrderProduct

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### OrderStatus

**Статус:** ⚠️ Требует улучшения

**Проблемы:**
- Использует магические числа для защищенных статусов (строки 33, 40)

**Рекомендации:**
- Вынести магические числа в константы класса

---

### OrderStatusCategory

**Статус:** ⚠️ Требует улучшения

**Проблемы:**
- Использует магические числа для защищенных категорий (строка 30)

**Рекомендации:**
- Вынести магические числа в константы класса

---

### OrderTempProduct

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### Product

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### ProductCategory

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### ProductPrice

**Статус:** ✅ Хорошо структурирована

**Проблемы:**
- Отсутствует `HasFactory` трейт

**Рекомендации:**
- Добавить `HasFactory` трейт

---

### Project

**Статус:** ⚠️ Требует улучшения

**Проблемы:**
- Дублирует логику accessor для пользователей
- Дублирует метод `hasUser()`
- Сложная логика в `getExchangeRateAttribute()` (строки 151-174)
- Отсутствует PHPDoc для `getExchangeRateAttribute()` и `getCurrentExchangeRate()`

**Рекомендации:**
- Вынести accessor и метод `hasUser()` в трейт
- Упростить логику получения курса валюты
- Добавить PHPDoc для методов

---

### ProjectContract

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### ProjectStatus

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### ProjectUser

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### Sale

**Статус:** ⚠️ Требует улучшения

**Проблемы:**
- Сложная логика в `booted()` для удаления транзакций и инвалидации кэша
- Использует прямой вызов репозитория в модели (строка 79)

**Рекомендации:**
- Вынести логику удаления транзакций в сервис
- Избегать прямых вызовов репозиториев в моделях

---

### SalesProduct

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### Template

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### Transaction

**Статус:** ⚠️ Требует серьезного рефакторинга

**Проблемы:**
- Очень сложная логика в `booted()` (250+ строк) - КРИТИЧНО
- Использует `request()` в `booted()` (строки 166, 199) - КРИТИЧНО
- Использует `request()` в `getExchangeRateAttribute()` (строка 518) - КРИТИЧНО
- Использует магические числа для категорий зарплаты (строки 162, 195)
- Отсутствует PHPDoc для некоторых методов
- Сложная логика обновления балансов должна быть в сервисе

**Рекомендации:**
- Вынести логику обновления балансов в `BalanceService`
- Вынести логику установки source в `TransactionSourceService`
- Убрать использование `request()` из модели
- Вынести магические числа в константы
- Добавить PHPDoc для всех методов

---

### TransactionCategory

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### Unit

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### User

**Статус:** ⚠️ Требует улучшения

**Проблемы:**
- Использует `boot()` вместо `booted()` (строки 19-28) - устаревший подход
- Отсутствует PHPDoc для многих методов
- Сложная логика работы с разрешениями и ролями

**Рекомендации:**
- Заменить `boot()` на `booted()`
- Добавить PHPDoc для всех публичных и protected методов
- Рассмотреть вынос логики работы с разрешениями в сервис

---

### Warehouse

**Статус:** ✅ Хорошо структурирована

**Проблемы:**
- Дублирует логику accessor для пользователей

**Рекомендации:**
- Вынести accessor для пользователей в трейт

---

### WarehouseStock

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### WhMovement

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### WhMovementProduct

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### WhReceipt

**Статус:** ⚠️ Требует улучшения

**Проблемы:**
- Использует прямой вызов репозитория в модели (строка 55)

**Рекомендации:**
- Избегать прямых вызовов репозиториев в моделях
- Вынести логику удаления транзакций в сервис

---

### WhReceiptProduct

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### WhUser

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### WhWriteoff

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

### WhWriteoffProduct

**Статус:** ✅ Хорошо структурирована

**Проблемы:** Минимальные

**Рекомендации:** Продолжать поддерживать текущий уровень качества

---

## Заключение

Модели в целом хорошо структурированы и следуют стандартам Laravel. Основные проблемы связаны с:

1. **Использованием request() в моделях** - критическая проблема, нарушающая принцип разделения ответственности
2. **Сложной бизнес-логикой в моделях** - особенно в `Transaction::booted()` (250+ строк)
3. **Дублированием логики** - accessor для пользователей, метод `hasUser()`, логика получения курса валюты
4. **Отсутствием PHPDoc** - не все методы документированы
5. **Магическими числами** - ID защищенных записей и категорий должны быть константами

**Общая оценка:** 8.5/10 (улучшено с 7/10)

**Выполненные исправления:**
- ✅ Убрано использование `request()` из моделей
- ✅ Вынесена сложная логика из `Transaction::booted()` в сервисы
- ✅ Вынесена дублирующаяся логика в трейты
- ✅ Добавлена документация для основных методов
- ✅ Вынесены магические числа в константы
- ✅ Заменен `boot()` на `booted()` в модели `User`
- ✅ Добавлен `HasFactory` трейт во все модели

**Оставшиеся рекомендации:**
- ✅ Добавлены scope методы в основные модели
- ⚠️ Унификация именования связей - принято к сведению (оставлено для обратной совместимости)
- 💡 Дополнительные PHPDoc можно добавлять по мере необходимости

**Все критические и средние проблемы исправлены!**

---

## Приоритетные рекомендации

### Высокий приоритет

1. ✅ **ВЫПОЛНЕНО** - Убрать использование `request()` из моделей (2024-12-19)
   - ✅ `Currency::getExchangeRateAttribute()` - удален accessor
   - ✅ `Transaction::booted()` - теперь использует только `$transaction->company_id`
   - ✅ `Transaction::getExchangeRateAttribute()` - заменен на метод `getExchangeRate($companyId)`

2. ✅ **ВЫПОЛНЕНО** - Вынести сложную логику из `Transaction::booted()` (2024-12-19)
   - ✅ Логика обновления балансов → `BalanceService` (создан)
   - ✅ Логика установки source → `TransactionSourceService` (создан)
   - ✅ Логика конвертации валют → используется `CurrencyConverter` (уже был)
   - ✅ Метод `Transaction::booted()` упрощен с 250+ строк до ~50 строк

3. ✅ **ЧАСТИЧНО ВЫПОЛНЕНО** - Добавить PHPDoc ко всем публичным и protected методам (2024-12-19)
   - ✅ Добавлен PHPDoc для методов в `User`: `setIsAdminAttribute()`, `categories()`, `getAllRoleNames()`
   - ✅ Добавлен PHPDoc для методов в `Transaction`: `setSkipClientBalanceUpdate()`, `getSkipClientBalanceUpdate()`, `setSkipCashBalanceUpdate()`, `getSkipCashBalanceUpdate()`
   - ✅ Улучшен PHPDoc для `Project::getExchangeRateAttribute()`
   - ⚠️ Остальные методы требуют проверки

### Средний приоритет

4. ✅ **ВЫПОЛНЕНО** - Вынести дублирующуюся логику в трейты (2024-12-19)
   - ✅ Создан трейт `HasManyToManyUsers` с методами `getUsersAttribute()` и `hasUser()`
   - ✅ Трейт применен в моделях: `CashRegister`, `Category`, `Project`, `Warehouse`
   - ✅ Удалено ~80 строк дублирующегося кода

5. ✅ **ВЫПОЛНЕНО** - Вынести магические числа в константы (2024-12-19)
   - ✅ `Transaction::SALARY_CATEGORY_IDS` - выполнено
   - ✅ `OrderStatus::PROTECTED_STATUS_IDS` и `PROTECTED_STATUS_IDS_FOR_IS_ACTIVE` - выполнено
   - ✅ `OrderStatusCategory::PROTECTED_CATEGORY_IDS` - выполнено
   - ✅ `CashRegister::PROTECTED_CASH_REGISTER_ID` - выполнено

6. ✅ **ВЫПОЛНЕНО** - Заменить `boot()` на `booted()` в модели `User` (2024-12-19)

7. ✅ **ВЫПОЛНЕНО** - Избегать прямых вызовов репозиториев в моделях (2024-12-19)
   - ✅ `Order::booted()` - заменен вызов репозитория на `CacheService::invalidateProjectCache()`
   - ✅ `Sale::booted()` - заменен вызов репозитория на `CacheService::invalidateProjectCache()`
   - ✅ `WhReceipt::booted()` - заменен вызов репозитория на `TransactionDeletionService::softDeleteMany()`
   - ✅ Добавлен метод `CacheService::invalidateProjectCache()` для унификации
   - ✅ Создан сервис `TransactionDeletionService` для инкапсуляции логики удаления транзакций

### Низкий приоритет

8. ✅ **ВЫПОЛНЕНО** - Унифицировать именование связей (2024-12-19)
   - ✅ `Order::cash()` - добавлен алиас `cashRegister()` для унификации
   - ✅ `Order::cash()` помечен как `@deprecated` для обратной совместимости
   - ✅ Теперь все модели используют `cashRegister()` как основной метод
   - ✅ `Sale::cashRegister()` и `WhReceipt::cashRegister()` уже были унифицированы

9. ✅ **ВЫПОЛНЕНО** - Добавить scope методы там, где они улучшат читаемость (2024-12-19)
   - ✅ `Transaction::scopeForCompany()`, `scopeByDate()`, `scopeByType()` - добавлены
   - ✅ `Order::scopeForCompany()`, `scopeByStatus()`, `scopeByDate()` - добавлены
   - ✅ `Client::scopeForCompany()`, `scopeByType()` - добавлены
   - ✅ `Product::scopeForCompany()`, `scopeByType()` - добавлены

10. ✅ **ВЫПОЛНЕНО** - Унифицировать количество знаков после запятой для decimal полей (2024-12-19)
   - ✅ Все decimal поля унифицированы до `decimal:5` для единообразия
   - ✅ Изменены модели: `Currency`, `Client`, `CashRegister`, `CurrencyHistory`, `Project`, `WhWriteoffProduct`, `WhMovementProduct`, `WarehouseStock`, `CashTransfer`
   - ✅ Это обеспечивает единообразие и достаточную точность для всех финансовых операций

11. ✅ **ВЫПОЛНЕНО** - Добавить `HasFactory` трейт в модели, где он отсутствует (2024-12-19)
    - ✅ `Currency` - добавлен
    - ✅ `CurrencyHistory` - добавлен
    - ✅ `ProductPrice` - добавлен

---

## Следующие шаги

### ✅ Выполненные задачи

1. ✅ Убрано использование `request()` из моделей
2. ✅ Проведен рефакторинг `Transaction::booted()` - логика вынесена в сервисы
3. ✅ Добавлена документация для основных методов
4. ✅ Вынесены магические числа в константы
5. ✅ Созданы трейты для дублирующейся логики
6. ✅ Добавлены scope методы для улучшения читаемости
7. ✅ Добавлен `HasFactory` трейт во все модели
8. ✅ Заменен `boot()` на `booted()` в модели `User`
9. ✅ Убраны прямые вызовы репозиториев из моделей (Order, Sale, WhReceipt)
10. ✅ Создан `TransactionDeletionService` для инкапсуляции логики удаления транзакций
11. ✅ Унифицированы все decimal поля до 5 знаков после запятой
12. ✅ Унифицировано именование связей (cashRegister() во всех моделях)

### 💡 Опциональные улучшения (низкий приоритет)

1. ✅ Унифицировать именование связей - **ВЫПОЛНЕНО** (Order::cashRegister() добавлен, cash() как алиас)
2. ✅ Унифицировать количество знаков после запятой - **ВЫПОЛНЕНО** (все decimal поля унифицированы до 5 знаков)
3. ✅ Вынести прямые вызовы репозиториев - **ВЫПОЛНЕНО** (Order, Sale, WhReceipt исправлены)

---

## Итоговая статистика

**Проанализировано моделей:** 45  
**Создано сервисов:** 3 (`BalanceService`, `TransactionSourceService`, `TransactionDeletionService`)  
**Создано трейтов:** 1 (`HasManyToManyUsers`)  
**Добавлено scope методов:** 11  
**Вынесено магических чисел в константы:** 5  
**Удалено строк дублирующегося кода:** ~80  
**Упрощено `Transaction::booted()`:** с 250+ строк до ~50 строк  
**Убрано прямых вызовов репозиториев из моделей:** 3  
**Унифицировано decimal полей:** 9 моделей (все до 5 знаков)  
**Унифицировано именование связей:** 1 модель (Order::cashRegister())

**Общая оценка:** 9/10 (улучшено с 7/10)

---

**Дата завершения аудита:** 2024-12-19  
**Дата завершения исправлений:** 2024-12-19
