# Отчет по аудиту контроллеров Laravel

**Дата аудита:** 2024  
**Проанализировано контроллеров:** 34  
**Базовый контроллер:** `App\Http\Controllers\Controller`

---

## Общая статистика

-   **Всего контроллеров:** 34
-   **API контроллеров:** 33
-   **Web контроллеров:** 1
-   **Контроллеров с полным PHPDoc:** ~60%
-   **Контроллеров с частичным PHPDoc:** ~30%
-   **Контроллеров без PHPDoc:** ~10%

---

## Критические проблемы

### 1. Отсутствие PHPDoc в методах ✅ (Частично исправлено)

**Проблема:** Согласно правилам проекта, все методы Laravel должны содержать PHPDoc. В некоторых контроллерах PHPDoc отсутствует или неполный.

**Статус исправления:**

-   ✅ `AuthController` - добавлен PHPDoc ко всем методам (`login()`, `me()`, `logout()`, `refresh()`, конструктору и свойству)
-   ⏳ Остальные контроллеры - требуется проверка и добавление PHPDoc

**Примеры (исправлено):**

-   ✅ `AuthController::login()` - добавлен PHPDoc
-   ✅ `AuthController::me()` - добавлен PHPDoc
-   ✅ `AuthController::logout()` - добавлен PHPDoc
-   ✅ `AuthController::refresh()` - добавлен PHPDoc

**Рекомендация:** Продолжить добавление PHPDoc в остальных контроллерах.

---

### 2. Дублирование логики валидации ✅ (Частично исправлено)

**Проблема:** Логика валидации дублируется в методах `store()` и `update()`.

**Статус исправления:**

-   ✅ `UsersController` - созданы `StoreUserRequest` и `UpdateUserRequest`, валидация вынесена в Form Request классы
-   ✅ `CompaniesController` - созданы `StoreCompanyRequest` и `UpdateCompanyRequest`, валидация вынесена в Form Request классы
-   ✅ `ClientController` - созданы `StoreClientRequest` и `UpdateClientRequest`, валидация вынесена в Form Request классы
-   ⚠️ **Важно:** Исправлены 3 контроллера, чтобы не сломать фронтенд. Остальные контроллеры оставлены без изменений до тестирования.
-   ⏳ Остальные контроллеры - требуется рефакторинг после тестирования

**Примеры (исправлено):**

-   ✅ `UsersController::store()` и `update()` - валидация вынесена в Form Request классы
-   ✅ `CompaniesController::store()` и `update()` - валидация вынесена в Form Request классы
-   ✅ `ClientController::store()` и `update()` - валидация вынесена в Form Request классы

**Что сделано:**

-   Созданы 6 Form Request класса с полной логикой нормализации данных
-   Сохранен формат ответов ошибок для совместимости с фронтендом
-   Убрано дублирование логики нормализации данных между методами
-   Добавлены casts для boolean полей в модели `Client` (`is_supplier`, `is_conflict`, `status`)
-   Созданы тесты для `ClientController` (15 тестов, все проходят)

**Рекомендация:** После тестирования продолжить вынесение валидации в Form Request классы для остальных контроллеров.

---

### 3. Непоследовательная обработка ошибок

**Проблема:** В разных контроллерах используются разные подходы к обработке ошибок.

**Примеры:**

-   `OrderController::store()` - использует `try-catch` с `\Throwable`
-   `ClientController::store()` - использует `try-catch` с `\Throwable` и `DB::beginTransaction()`
-   `UsersController::store()` - не использует `try-catch`, полагается на валидацию
-   `ProductController::store()` - не использует `try-catch`

**Рекомендация:** Унифицировать подход к обработке ошибок. Использовать единый механизм обработки исключений.

---

### 4. Дублирование логики проверки прав доступа

**Проблема:** Проверка прав доступа к кассам и складам дублируется в нескольких контроллерах.

**Примеры:**

-   `OrderController::store()` - проверка `checkCashRegisterAccess()` и `checkWarehouseAccess()`
-   `OrderController::update()` - та же проверка
-   `SaleController::store()` - та же проверка

**Рекомендация:** Логика уже вынесена в базовый контроллер, что хорошо. Но можно рассмотреть использование middleware для автоматической проверки.

---

### 5. Непоследовательное использование валидации

**Проблема:** В разных контроллерах используются разные способы валидации.

**Примеры:**

-   `UsersController::store()` - использует `Validator::make()` и `validationErrorResponse()`
-   `OrderController::store()` - использует `$request->validate()`
-   `CompaniesController::store()` - использует `Validator::make()` и `validationErrorResponse()`

**Рекомендация:** Унифицировать подход. Рекомендуется использовать Form Request классы для сложной валидации.

---

### 6. Лишние проверки и избыточная валидация

**Проблема:** В контроллерах присутствуют избыточные проверки, которые дублируют логику валидации или проверки прав доступа.

**Примеры:**

-   Двойная проверка существования записи: сначала `find()`, затем проверка на `null`, хотя можно использовать `findOrFail()`
-   Проверка прав доступа в нескольких местах одного метода
-   Валидация данных, которые уже проверены в Form Request классах
-   Проверка `isset()` для полей, которые гарантированно присутствуют после валидации
-   Избыточные проверки типов данных, которые уже обработаны через casts в моделях

**Рекомендация:** 
- Убрать дублирующие проверки
- Использовать `findOrFail()` вместо `find()` + проверка на `null`
- Убрать проверки полей после валидации в Form Request классах
- Полагаться на casts моделей для типизации данных
- Вынести повторяющиеся проверки в приватные методы или middleware

---

### 7. Использование устаревших методов Laravel

**Проблема:** В контроллерах используются устаревшие методы и подходы, которые не рекомендуются в современных версиях Laravel.

**Примеры:**

-   Использование `Validator::make()` вместо Form Request классов
-   Использование `DB::table()` вместо Eloquent моделей
-   Использование `collect()` для простых операций, где достаточно массива
-   Использование устаревших методов работы с файлами
-   Использование `response()->json()` вместо методов базового контроллера
-   Использование `$request->has()` вместо `$request->filled()` для проверки наличия значения

**Рекомендация:**
- Заменить `Validator::make()` на Form Request классы
- Использовать Eloquent вместо Query Builder где возможно
- Использовать методы базового контроллера (`successResponse()`, `errorResponse()`, `paginatedResponse()`)
- Использовать `$request->filled()` вместо `$request->has()` для проверки наличия значения
- Проверить актуальность используемых методов согласно документации Laravel

---

## Средние проблемы

### 6. Дублирование логики обработки данных

**Проблема:** Логика нормализации и обработки данных дублируется.

**Примеры:**

-   `UsersController::store()` и `update()` - обработка `is_active`, `is_admin`, `roles`, `companies`, `company_roles`, `position`, `hire_date`, `birthday`
-   `CompaniesController::store()` и `update()` - обработка `rounding_enabled`, `rounding_custom_threshold`, `rounding_quantity_enabled`

**Рекомендация:** Вынести в приватные методы или использовать мутаторы в моделях.

---

### 7. Непоследовательное использование транзакций БД

**Проблема:** Транзакции используются не везде, где это необходимо.

**Примеры:**

-   `ClientController::store()` - использует `DB::beginTransaction()`
-   `OrderController::store()` - не использует транзакции (хотя создает связанные записи)
-   `UsersController::store()` - не использует транзакции

**Рекомендация:** Использовать транзакции для операций, которые создают/обновляют несколько связанных записей.

---

### 8. Непоследовательное использование кеширования

**Проблема:** Инвалидация кеша вызывается в разных местах и не всегда последовательно.

**Примеры:**

-   `OrderController::store()` - инвалидирует несколько кешей
-   `ClientController::store()` - инвалидирует несколько кешей
-   `CategoriesController::store()` - инвалидирует только один кеш

**Рекомендация:** Использовать события моделей для автоматической инвалидации кеша.

---

### 9. Непоследовательное именование переменных и методов

**Проблема:** В разных контроллерах используются разные стили именования переменных, методов и классов.

**Примеры:**

-   **Переменные:**
    -   `OrderController` - использует `$userUuid`
    -   `UsersController` - использует `$currentUser`, `$targetUser`
    -   `ProductController` - использует `$userUuid`
-   **Методы:**
    -   Некоторые контроллеры используют `getItemById()`, другие - `find()`, третьи - `findOrFail()`
    -   Разные названия для похожих методов: `getAllUsers()`, `all()`, `index()`
-   **Классы:**
    -   `CategoriesController` vs `CategoryController` (непоследовательность в единственном/множественном числе)
    -   `TransfersController` vs `TransferController`
    -   `ProjectsController` vs `ProjectController`

**Рекомендация:** 
- Унифицировать именование переменных согласно PSR стандартам
- Использовать единый стиль именования методов (например, `findOrFail()` везде вместо `find()` + проверка)
- Привести к единому стилю именования контроллеров (рекомендуется использовать множественное число: `UsersController`, `OrdersController`)
- Использовать единые названия для похожих методов во всех контроллерах

---

### 10. Отсутствие проверки существования записей перед операциями

**Проблема:** В некоторых местах используется `findOrFail()`, в других - `find()` с ручной проверкой.

**Примеры:**

-   `OrderController::update()` - использует `getItemById()` и проверяет на `null`
-   `ClientController::update()` - использует `Client::find()` и проверяет на `null`
-   `ProductController::update()` - использует `Product::findOrFail()`

**Рекомендация:** Использовать `findOrFail()` везде, где это возможно, для единообразия.

---

## Мелкие проблемы

### 11. Непоследовательное форматирование ответов

**Проблема:** Формат JSON ответов различается между контроллерами.

**Примеры:**

-   `OrderController::store()` - возвращает `['message' => '...']`
-   `UsersController::store()` - возвращает `userResponse($user)` с более сложной структурой
-   `ProductController::store()` - возвращает `['item' => ..., 'message' => '...']`

**Рекомендация:** Использовать единый формат ответов или API Resource классы.

---

### 12. Непоследовательное использование методов базового контроллера

**Проблема:** Не все контроллеры используют методы базового контроллера для ответов.

**Примеры:**

-   `OrderController::index()` - использует `response()->json()` напрямую
-   `UsersController::index()` - использует `paginatedResponse()`
-   `ProductController::products()` - использует `paginatedResponse()`

**Рекомендация:** Использовать методы базового контроллера (`successResponse()`, `paginatedResponse()`, `errorResponse()`) везде.

---

### 13. Отсутствие проверки прав доступа в некоторых методах

**Проблема:** Не все методы проверяют права доступа перед выполнением операций.

**Примеры:**

-   `CategoriesController::destroy()` - не проверяет права доступа
-   `WarehouseController::store()` - не проверяет права доступа
-   `WarehouseController::update()` - проверяет права только на обновление, но не на создание

**Рекомендация:** Добавить проверку прав доступа во все методы, которые изменяют данные.

---

### 14. Непоследовательное использование типов возвращаемых значений

**Проблема:** В PHPDoc не всегда указаны типы возвращаемых значений.

**Примеры:**

-   `OrderController::index()` - указан `\Illuminate\Http\JsonResponse`
-   `UsersController::index()` - указан `\Illuminate\Http\JsonResponse`
-   `AuthController::login()` - не указан тип возвращаемого значения

**Рекомендация:** Добавить типы возвращаемых значений во все PHPDoc комментарии.

---

### 15. Дублирование логики обработки файлов

**Проблема:** Логика загрузки и удаления файлов дублируется.

**Примеры:**

-   `UsersController::handlePhotoUpload()` - приватный метод для обработки фото
-   `ProductController::store()` и `update()` - обработка изображений прямо в методах
-   `CompaniesController::store()` и `update()` - обработка логотипа прямо в методах

**Рекомендация:** Вынести логику обработки файлов в отдельный сервис или использовать трейты.

---

## Положительные моменты

### ✅ Хорошие практики

1. **Использование репозиториев** - все контроллеры используют репозитории для работы с данными
2. **Базовый контроллер** - хорошо структурированный базовый контроллер с полезными методами
3. **Проверка прав доступа** - большинство контроллеров проверяют права доступа через `canPerformAction()`
4. **Использование кеширования** - контроллеры инвалидируют кеш после изменений
5. **Валидация данных** - все контроллеры валидируют входящие данные

---

## Рекомендации по улучшению

### Приоритет 1 (Критично)

1. **Добавить PHPDoc ко всем методам** - особенно в `AuthController` и других контроллерах без документации
2. **Унифицировать обработку ошибок** - использовать единый подход с try-catch или глобальный обработчик
3. **Вынести дублирующуюся логику** - создать приватные методы или использовать Form Request классы
4. **Убрать лишние проверки** - избавиться от избыточной валидации и дублирующих проверок
5. **Заменить устаревшие методы** - использовать современные подходы Laravel вместо устаревших методов

### Приоритет 2 (Важно)

6. **Использовать Form Request классы** - для сложной валидации вместо inline валидации
7. **Добавить транзакции БД** - для операций, которые изменяют несколько связанных записей
8. **Унифицировать формат ответов** - использовать API Resource классы или единый формат
9. **Привести к единому стилю именования** - унифицировать названия переменных, методов и контроллеров

### Приоритет 3 (Желательно)

10. **Использовать события моделей** - для автоматической инвалидации кеша
11. **Создать сервисы для обработки файлов** - вынести логику работы с файлами
12. **Добавить middleware для проверки прав** - автоматизировать проверку прав доступа к ресурсам

---

## Примеры улучшений

### Пример 1: Добавление PHPDoc

**Было:**

```php
public function login(Request $request)
{
    // ...
}
```

**Должно быть:**

```php
/**
 * Аутентификация пользователя
 *
 * @param Request $request
 * @return \Illuminate\Http\JsonResponse
 */
public function login(Request $request)
{
    // ...
}
```

### Пример 2: Вынесение дублирующейся логики

**Было:**

```php
// В методе store()
if (isset($data['is_active'])) {
    $data['is_active'] = filter_var($data['is_active'], FILTER_VALIDATE_BOOLEAN);
}
// В методе update() - та же логика
```

**Должно быть:**

```php
/**
 * Нормализовать данные пользователя
 *
 * @param array $data
 * @return array
 */
protected function normalizeUserData(array $data): array
{
    if (isset($data['is_active'])) {
        $data['is_active'] = filter_var($data['is_active'], FILTER_VALIDATE_BOOLEAN);
    }
    // ... остальная логика
    return $data;
}
```

### Пример 3: Использование Form Request

**Было:**

```php
public function store(Request $request)
{
    $request->validate([
        'name' => 'required|string',
        'email' => 'required|email|unique:users,email',
        // ...
    ]);
}
```

**Должно быть:**

```php
public function store(StoreUserRequest $request)
{
    // Валидация уже выполнена в StoreUserRequest
}
```

---

## Список всех контроллеров проекта

### API контроллеры (33)

1. `App\Http\Controllers\Api\AppController`
2. `App\Http\Controllers\Api\AuthController`
3. `App\Http\Controllers\Api\CacheController`
4. `App\Http\Controllers\Api\CashRegistersController`
5. `App\Http\Controllers\Api\CategoriesController`
6. `App\Http\Controllers\Api\ClientController`
7. `App\Http\Controllers\Api\CommentController`
8. `App\Http\Controllers\Api\CompaniesController`
9. `App\Http\Controllers\Api\CompanyRoundingRulesController`
10. `App\Http\Controllers\Api\CurrencyHistoryController`
11. `App\Http\Controllers\Api\InvoiceController`
12. `App\Http\Controllers\Api\OrderController`
13. `App\Http\Controllers\Api\OrderStatusCategoryController`
14. `App\Http\Controllers\Api\OrderStatusController`
15. `App\Http\Controllers\Api\OrderTransactionController`
16. `App\Http\Controllers\Api\PerformanceController`
17. `App\Http\Controllers\Api\ProductController`
18. `App\Http\Controllers\Api\ProjectContractsController`
19. `App\Http\Controllers\Api\ProjectsController`
20. `App\Http\Controllers\Api\ProjectStatusController`
21. `App\Http\Controllers\Api\RolesController`
22. `App\Http\Controllers\Api\SaleController`
23. `App\Http\Controllers\Api\TransactionCategoryController`
24. `App\Http\Controllers\Api\TransactionsController`
25. `App\Http\Controllers\Api\TransfersController`
26. `App\Http\Controllers\Api\UserCompanyController`
27. `App\Http\Controllers\Api\UsersController`
28. `App\Http\Controllers\Api\WarehouseController`
29. `App\Http\Controllers\Api\WarehouseMovementController`
30. `App\Http\Controllers\Api\WarehouseReceiptController`
31. `App\Http\Controllers\Api\WarehouseStockController`
32. `App\Http\Controllers\Api\WarehouseWriteoffController`

### Web контроллеры (1)

33. `App\Http\Controllers\DashboardController`

### Базовый контроллер (1)

34. `App\Http\Controllers\Controller`

---

## Заключение

Контроллеры в целом хорошо структурированы и следуют основным принципам Laravel. Основные проблемы связаны с:

1. Отсутствием PHPDoc в некоторых методах
2. Дублированием логики между методами
3. Непоследовательностью в обработке ошибок и валидации
4. Наличием лишних проверок и избыточной валидации
5. Использованием устаревших методов Laravel
6. Непоследовательностью в именовании переменных, методов и контроллеров

Рекомендуется провести рефакторинг с фокусом на:

-   Добавление PHPDoc
-   Вынесение дублирующейся логики
-   Унификацию подходов к валидации и обработке ошибок
-   Использование Form Request классов для сложной валидации
-   Удаление лишних проверок и избыточной валидации
-   Замену устаревших методов на современные подходы Laravel
-   Приведение к единому стилю именования переменных, методов и контроллеров

---

---

## Прогресс исправлений

### Выполнено ✅

1. **Добавлен PHPDoc в AuthController** (2024)

    - Добавлен PHPDoc ко всем публичным методам
    - Добавлен PHPDoc к конструктору и свойству
    - Проверено линтером - ошибок нет

2. **Вынесена валидация в Form Request классы** (2024)

    - **Что проверено:**
        - Проверены все контроллеры на наличие дублирования валидации в методах `store()` и `update()`
        - Выявлено, что большинство контроллеров используют inline валидацию
    - **Что исправлено:**
        - Созданы `StoreUserRequest` и `UpdateUserRequest` для `UsersController`
            - Вынесена вся логика валидации из методов `store()` и `update()`
            - Добавлена нормализация данных: boolean поля, строки в массивы, пустые строки в null
            - Сохранен формат ответов ошибок для совместимости с фронтендом
        - Созданы `StoreCompanyRequest` и `UpdateCompanyRequest` для `CompaniesController`
            - Вынесена вся логика валидации из методов `store()` и `update()`
            - Добавлена нормализация данных
            - Сохранен формат ответов ошибок для совместимости с фронтендом
    - **Результат:**
        - Убрано дублирование логики нормализации данных между методами
        - Проверено линтером - ошибок нет
        - Созданы и успешно проходят Feature тесты для проверки валидации и нормализации
    - ⚠️ **Ограничение:** Исправлены только 2 контроллера (`UsersController` и `CompaniesController`) для безопасного тестирования на фронтенде перед массовым рефакторингом

3. **Созданы автотесты для контроллеров** (2024)

    - **Что проверено:**
        - Проверена работа валидации в методах `store()` и `update()`
        - Проверена нормализация данных (boolean, пустые строки, строки в массивы)
        - Проверена обработка ошибок (404, 403, 422)
        - Проверена работа с правами доступа
    - **Что создано:**
        - Создан `UsersControllerTest` с 18 тестами для проверки валидации и нормализации данных
        - Создан `CompaniesControllerTest` с 9 тестами для проверки валидации и нормализации данных
        - Создана фабрика `CompanyFactory` для тестовых данных
    - **Что покрывают тесты:**
        - Валидацию обязательных полей (`test_store_user_requires_validation`, `test_update_user_requires_validation`)
        - Успешное создание/обновление (`test_store_user_with_valid_data`, `test_update_user_with_valid_data`)
        - Нормализацию boolean полей (строки 'true'/'false' в boolean) (`test_store_user_normalizes_boolean_fields`, `test_update_user_normalizes_boolean_fields`)
        - Нормализацию пустых строк в null (`test_store_user_normalizes_empty_strings_to_null`, `test_update_user_normalizes_empty_strings_to_null`)
        - Нормализацию строковых массивов в массивы (`test_store_user_normalizes_string_roles_to_array`, `test_store_user_normalizes_string_companies_to_array`)
        - Логику сброса полей при отключении функций (rounding) - в `CompaniesControllerTest`
        - Проверку прав доступа (`test_non_admin_cannot_set_is_admin`, `test_update_user_checks_permissions`, `test_destroy_user_checks_permissions`)
        - Обработку ошибок 404 (`test_destroy_user_returns_404_when_not_found`, `test_update_user_returns_404_when_not_found`)
        - Пагинацию (`test_index_returns_paginated_users`)
        - Удаление (`test_destroy_user_successfully`)
    - **Технические детали:**
        - Тесты используют `DatabaseTransactions` для изоляции (вместо `RefreshDatabase` для работы с существующей БД)
        - Создан метод `actingAsApi()` для правильной аутентификации API запросов через Sanctum токены
        - Исправлена проблема с типами данных: добавлены casts для `is_active` и `is_admin` в модель User
        - Создана миграция для изменения типа полей `is_active` и `is_admin` на boolean
        - Исправлена фабрика `CompanyFactory` (поле `logo` не может быть null)

4. **Исправлена проблема с типами данных boolean** (2024)
    - **Что проверено:**
        - Проверены все контроллеры на использование boolean полей
        - Проверены все модели на наличие casts для boolean полей
        - Проверена обработка boolean значений в контроллерах (нормализация строк в boolean)
    - **Результаты проверки:**
        - ✅ **User** (`is_active`, `is_admin`) - есть casts в модели, исправлено
        - ✅ **OrderStatus** (`is_active`) - есть cast в модели, в контроллере используется `(bool)$request->is_active`
        - ✅ **Transaction** (`is_debt`) - есть cast в модели
        - ✅ **ProjectContract** (`returned`) - есть cast в модели
        - ✅ **Currency** (`is_default`, `is_report`) - есть casts в модели
        - ✅ **Client** (`is_supplier`, `is_conflict`, `status`) - добавлены casts в модель, исправлено
    - **Что исправлено:**
        - Добавлены casts `'is_active' => 'boolean'` и `'is_admin' => 'boolean'` в модель User
        - Исправлена миграция `2025_12_06_134850_change_users_is_active_to_boolean.php` для изменения типа полей
        - В Form Request классах (`StoreUserRequest`, `UpdateUserRequest`) добавлена нормализация строковых boolean значений ('true'/'false') в boolean
    - **Что исправлено дополнительно:**
        - ✅ Модель `Client` - добавлены casts для полей `is_supplier`, `is_conflict`, `status`
        - ✅ Созданы Form Request классы для `ClientController` (`StoreClientRequest`, `UpdateClientRequest`)
        - ✅ Созданы тесты для `ClientController` (15 тестов, все проходят)
        - ⚠️ **Важно:** Если в будущем возникнет аналогичная проблема (поле хранится как integer, а должно быть boolean), необходимо:
            1. Добавить cast в модель: `'field_name' => 'boolean'` в массив `$casts`
            2. Создать миграцию для изменения типа поля: `$table->boolean('field_name')->default(false)->change();`
            3. Выполнить миграцию: `php artisan migrate`

### В процессе ⏳

3. Добавление PHPDoc в остальные контроллеры

### Ожидает выполнения 📋

4. Вынесение валидации в Form Request для остальных контроллеров (после тестирования)
5. Непоследовательная обработка ошибок
6. **Создание миграции для boolean полей в модели Client** ✅
    - ✅ Добавлены casts для boolean полей в модель `Client` (`is_supplier`, `is_conflict`, `status`)
    - ✅ Создана миграция `2025_12_06_175143_change_clients_boolean_fields_to_boolean.php` для изменения типа полей в БД
7. Удаление лишних проверок и избыточной валидации
8. Замена устаревших методов на современные подходы Laravel
9. Приведение к единому стилю именования переменных, методов и контроллеров
10. Остальные пункты из списка проблем

---

## План покрытия тестами и рефакторинга

### ⚠️ Важно: Порядок выполнения

**Перед полным покрытием тестами необходимо выполнить рефакторинг на API Resources:**

1. **Переход на API Resources (бэкенд)** - по одному модулю

    - Создать API Resource классы для каждого модуля
    - Обновить контроллеры для использования API Resources
    - Сохранить обратную совместимость с текущим форматом ответов (если возможно)
    - ⚠️ **Важно:** Делать по одному модулю, чтобы не сломать фронтенд

2. **Обновление фронтенда** - после каждого модуля

    - Обновить фронтенд для работы с новым форматом API Resources
    - Протестировать функциональность
    - Убедиться, что ничего не сломано

3. **Покрытие тестами** - после рефакторинга
    - Написать полные Feature тесты для всех методов контроллера
    - Покрыть все сценарии: успешные операции, валидация, права доступа, ошибки
    - Использовать `DatabaseTransactions` для изоляции тестов

### Текущее состояние покрытия тестами

**UsersController** (пример):

-   ✅ `store()` - покрыт тестами (валидация, нормализация данных, успешное создание)
-   ✅ `update()` - покрыт тестами (валидация, нормализация, права доступа, ошибки)
-   ✅ `destroy()` - покрыт тестами (успешное удаление, права доступа, 404)
-   ✅ `index()` - покрыт тестами (пагинация)
-   ❌ `checkPermissions()` - не покрыт
-   ❌ `permissions()` - не покрыт
-   ❌ `roles()` - не покрыт
-   ❌ `getAllUsers()` - не покрыт
-   ❌ `getCurrentUser()` - не покрыт
-   ❌ `updateProfile()` - не покрыт (включая смену пароля)
-   ❌ `getSalaries()` - не покрыт
-   ❌ `createSalary()` - не покрыт
-   ❌ `updateSalary()` - не покрыт
-   ❌ `deleteSalary()` - не покрыт
-   ❌ `getEmployeeBalance()` - не покрыт
-   ❌ `getEmployeeBalanceHistory()` - не покрыт
-   ❌ Загрузка фото в `store()` и `update()` - не покрыта

**ClientController**:

-   ✅ `store()` - покрыт тестами (валидация, нормализация boolean полей, нормализация пустых строк в null, нормализация строковых телефонов в массив)
-   ✅ `update()` - покрыт тестами (валидация, нормализация boolean полей, нормализация пустых строк в null)
-   ✅ `destroy()` - покрыт тестами (успешное удаление, проверка баланса, 404)
-   ✅ `show()` - покрыт тестами (успешное получение, 404)
-   ✅ `index()` - покрыт тестами (пагинация)
-   ❌ `search()` - не покрыт
-   ❌ `all()` - не покрыт
-   ❌ `getBalanceHistory()` - не покрыт

**Рекомендация:** После перехода на API Resources и обновления фронтенда добавить полное покрытие тестами для всех методов.

---

## Важные заметки для будущих исправлений

### Проблема с типами данных boolean

**Проблема:** Поля, которые должны быть boolean, могут храниться в БД как integer (0/1), что вызывает проблемы в тестах и коде.

**Симптомы:**

-   В тестах `assertTrue($model->field)` падает с ошибкой "Failed asserting that 1 is true"
-   В коде поле возвращает `1` или `0` вместо `true` или `false`

**Решение:**

1. **Добавить cast в модель:**

```php
protected $casts = [
    'field_name' => 'boolean',
];
```

2. **Создать миграцию для изменения типа поля:**

```php
Schema::table('table_name', function (Blueprint $table) {
    $table->boolean('field_name')->default(false)->change();
});
```

3. **Выполнить миграцию:**

```bash
php artisan migrate
```

**Пример:** Исправлено для полей `is_active` и `is_admin` в модели `User`:

-   Добавлены casts в модель `User`
-   Исправлена миграция `2025_12_06_134850_change_users_is_active_to_boolean.php`
-   После миграции поля корректно работают как boolean

**Найдена аналогичная проблема:**

-   ❌ Модель `Client` - поля `is_supplier`, `is_conflict`, `status` не имеют casts
-   Эти поля используются в `ClientController` с валидацией `'sometimes|nullable|boolean'`
-   Требуется добавить casts и создать миграцию (см. пункт 6 в разделе "Ожидает выполнения")

**Важно:** Если в будущем возникнет аналогичная проблема с другими полями, использовать этот же подход.

---

**Составлено:** AI Assistant  
**Дата:** 2024  
**Последнее обновление:** 2024
