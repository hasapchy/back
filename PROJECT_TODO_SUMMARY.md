# Сводный список задач по проекту

*Обновлено: 2024-12-19*

---

## 📊 Статус выполнения

### ✅ Выполнено:
- **Репозитории:** Убраны избыточные проверки и дублирование кода в 14 репозиториях
  - Удалено ~200 строк дублирующегося кода
  - Упрощено ~30 избыточных проверок
  - Создан метод `applySourceFilter()` в `CahRegistersRepository`

---

## 🔴 Критичные задачи (Приоритет 1)

### Репозитории

#### 1. OrdersRepository
- [ ] Разбить метод `getItemsWithPagination()` (300+ строк) на несколько методов
- [ ] Вынести дублирование логики фильтрации по кассам
- [ ] Разбить сложную логику обновления продуктов на методы
- [ ] Вынести дублирование логики расчета цен

#### 2. TransactionsRepository
- [ ] Разбить метод `getItemsWithPagination()` (270+ строк) на несколько методов
- [ ] Вынести сложную логику конвертации валют в отдельный метод
- [ ] Вынести дублирование логики обновления баланса кассы в метод

#### 3. UsersRepository
- [ ] Разбить метод `getItemsWithPagination()` на несколько методов
- [ ] Вынести дублирование логики фильтрации по компаниям
- [ ] Упростить логику проверки связанных данных через массив конфигурации

### Контроллеры

#### 4. Общие проблемы контроллеров
- [ ] Убрать дублирование проверок авторизации - использовать middleware
- [ ] Заменить `find()` + проверка на `findOrFail()` во всех контроллерах
- [ ] Убрать избыточные проверки после валидации
- [ ] Вынести повторяющуюся логику в методы/сервисы

#### 5. Специфичные контроллеры
- [ ] **ProjectsController**: Убрать дублирование проверок в `show()`
- [ ] **UsersController**: Использовать `findOrFail()`, вынести логику валидации пароля
- [ ] **OrderController**: Вынести логику проверки категорий, убрать избыточные проверки
- [ ] **TransactionsController**: Упростить логику определения `source_type` и `source_id`

---

## 🟡 Важные задачи (Приоритет 2)

### Репозитории

#### 6. ClientsRepository
- [ ] Вынести дублирование логики поиска в отдельный метод
- [ ] Разбить сложную логику в `getBalanceHistory()` на методы

#### 7. ProductsRepository
- [ ] Вынести дублирование логики получения остатков в метод
- [ ] Вынести дублирование логики обогащения продукта в метод

#### 8. ProjectsRepository
- [ ] Вынести дублирование логики трансформации в метод
- [ ] Разбить сложную логику в `getBalanceHistory()` на методы

#### 9. SalesRepository
- [ ] Упростить дублирование создания транзакций

#### 10. WarehouseReceiptRepository
- [ ] Упростить дублирование создания транзакций

#### 11. CategoriesRepository
- [ ] Вынести дублирование логики фильтрации по пользователям в метод

#### 12. LeaveRepository
- [ ] Вынести дублирование логики фильтрации в метод

#### 13. CommentsRepository
- [ ] Вынести дублирование проверки прав доступа в метод

#### 14. InvoicesRepository
- [ ] Вынести дублирование логики подготовки продуктов в метод

#### 15. WarehouseMovementRepository
- [ ] Вынести дублирование логики обновления остатков в метод

#### 16. WarehouseWriteoffRepository
- [ ] Упростить сложную логику обновления при смене склада

### Контроллеры

#### 17. Общие улучшения
- [ ] Разбить большие методы на более мелкие
- [ ] Использовать Resource классы для форматирования ответов
- [ ] Перенести бизнес-логику из контроллеров в сервисы
- [ ] Упростить формирование данных через `array_merge()` и `array_filter()`

---

## 🟢 Желательные задачи (Приоритет 3)

### Общие улучшения

#### 18. Качество кода
- [ ] Улучшить читаемость кода (разбить большие методы)
- [ ] Добавить PHPDoc комментарии
- [ ] Унифицировать стиль кода
- [ ] Заменить `array()` на `[]`
- [ ] Вынести константы в модели

#### 19. Обработка ошибок
- [ ] Исправить форматирование сообщений об ошибках
- [ ] Убрать избыточные try-catch блоки (Laravel автоматически обрабатывает исключения)

---

## 🔵 Функциональные задачи

### 20. Справочники с поддержкой системных и пользовательских записей

Нужно добавить поддержку `company_id` (nullable) в следующие справочники:

- [ ] **TransactionCategoryRepository**
  - [ ] Добавить миграцию для поля `company_id`
  - [ ] Обновить модель TransactionCategory
  - [ ] Обновить репозиторий для фильтрации по `company_id`
  - [ ] Обновить контроллер
  - [ ] Обновить кеширование

- [ ] **ProjectStatusRepository**
  - [ ] Добавить миграцию для поля `company_id`
  - [ ] Обновить модель ProjectStatus
  - [ ] Обновить репозиторий для фильтрации по `company_id`
  - [ ] Обновить контроллер
  - [ ] Обновить кеширование

- [ ] **OrderStatusRepository**
  - [ ] Добавить миграцию для поля `company_id`
  - [ ] Обновить модель OrderStatus
  - [ ] Обновить репозиторий для фильтрации по `company_id`
  - [ ] Обновить контроллер
  - [ ] Обновить кеширование

**Требования:**
- Системные записи (company_id = null) — общие для всех компаний
- Пользовательские записи (company_id != null) — свои для каждой компании
- Системные записи должны быть защищены от редактирования/удаления

---

## 📈 Статистика

### Репозитории
- **Всего репозиториев:** 28
- **Оптимизировано:** 14 (50%)
- **Осталось оптимизировать:** 14 (50%)
  - Критичные: 3 репозитория
  - Важные: 11 репозиториев

### Контроллеры
- **Всего контроллеров:** ~30+
- **Требуют оптимизации:** ~20+
- **Приоритетные задачи:** 4 общие + специфичные по контроллерам

### Функциональные задачи
- **Справочники:** 3 справочника требуют доработки
- **Миграции:** 3 миграции нужно создать

---

## 🎯 Рекомендуемый порядок выполнения

### Этап 1: Критичные задачи (1-2 недели)
1. Разбить большие методы в `OrdersRepository`, `TransactionsRepository`, `UsersRepository`
2. Убрать дублирование проверок авторизации в контроллерах
3. Заменить `find()` на `findOrFail()` во всех контроллерах

### Этап 2: Важные задачи (2-3 недели)
1. Вынести дублирование логики в репозиториях
2. Разбить сложную логику на методы
3. Перенести бизнес-логику из контроллеров в сервисы

### Этап 3: Функциональные задачи (1-2 недели)
1. Добавить поддержку `company_id` в справочники
2. Создать миграции и обновить модели
3. Обновить репозитории и контроллеры

### Этап 4: Улучшения качества (1 неделя)
1. Добавить PHPDoc комментарии
2. Унифицировать стиль кода
3. Улучшить обработку ошибок

---

## 📝 Примечания

- Все задачи из раздела "Критичные" должны быть выполнены в первую очередь
- Задачи из раздела "Важные" можно выполнять параллельно с критичными
- Задачи из раздела "Желательные" можно выполнять по мере возможности
- Функциональные задачи требуют планирования и тестирования

---

*Документ создан на основе:*
- `REPOSITORIES_OPTIMIZATION_REPORT.md`
- `CONTROLLERS_OPTIMIZATION_REPORT.md`
- `TODO.md`

